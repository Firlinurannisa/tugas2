import React from 'react';
import playlist from 'playlist';


const ListPlaylist = ({playlist}) =>
(
    <ul>
        {playlist.map((playlist) => 
            <playlist key={satuplaylist.image}playlist = {satuplaylist} />

        
        )}

    </ul>
)

export default ListPlaylist;
